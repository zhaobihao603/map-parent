<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.imap.cloud.common.dao.map.MapNodeMapper" >
    <resultMap id="BaseResultMap" type="com.imap.cloud.common.entity.map.MapNode" >
        <id column="id" property="id" jdbcType="CHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="x" property="x" jdbcType="DOUBLE" />
        <result column="y" property="y" jdbcType="DOUBLE" />
        <result column="area" property="area" jdbcType="CHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, name, x, y, area
    </sql>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.String" >
        select 
        <include refid="Base_Column_List" />
        from a_node
        where id = #{id,jdbcType=CHAR}
    </select>
    
    <select id="getByAreaId" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List" />
        from a_node
        where area = #{areaId,jdbcType=CHAR}
    </select>
    
    <select id="getByXY" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List" />
        from a_node
        where x = #{x,jdbcType=DOUBLE} and x = #{y,jdbcType=DOUBLE}
    </select>

    <insert id="insert" parameterType="com.imap.cloud.common.entity.map.MapNode" >
        insert into a_node (id, name, x, y, 
            area)
        values (#{id,jdbcType=CHAR}, #{name,jdbcType=VARCHAR}, #{x,jdbcType=DOUBLE}, #{y,jdbcType=DOUBLE}, 
            #{area,jdbcType=CHAR})
    </insert>

    <update id="updateById" parameterType="com.imap.cloud.common.entity.map.MapNode" >
        update a_node
        <set >
            <if test="name != null" >
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="x != null" >
                x = #{x,jdbcType=DOUBLE},
            </if>
            <if test="y != null" >
                y = #{y,jdbcType=DOUBLE},
            </if>
            <if test="area != null" >
                area = #{area,jdbcType=CHAR},
            </if>
        </set>
        where id = #{id,jdbcType=CHAR}
    </update>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.String" >
        delete from a_node
        where id = #{id,jdbcType=CHAR}
    </delete>
    
    <delete id="deleteByArea" parameterType="java.lang.String" >
        delete from a_node
        where area = #{areaId,jdbcType=CHAR}
    </delete>

</mapper>